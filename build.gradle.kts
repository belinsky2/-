// Пусто намеренно.
//
// Плагины не объявляются здесь через `apply false`: в JVM-режиме
// (PUNCHLINE_JVM_ONLY=1) Android-модули не подключаются, и Gradle не должен
// пытаться резолвить AGP из Google Maven, который в среде разработки закрыт.
// Каждый модуль объявляет свои плагины сам через version catalog.
