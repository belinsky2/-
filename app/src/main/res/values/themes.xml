<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Окно до старта Compose: чёрное, чтобы не было светлой вспышки при запуске. -->
    <style name="Theme.Punchline" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
